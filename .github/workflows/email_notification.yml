name: Email Notification

on:
  push:
    branches:
      - main
      - feature_a
      - feature_b

jobs:
  send_email:
    runs-on: ubuntu-latest
    steps:
    - name: Send email
      uses: dawidd6/action-send-mail@v3.12.0
      with:
        server_address: smtp.iteso.com
        server_port: 534
        username: tirzah.peniche@iteso.com
        password: KimSeokJinnie120304249203.
        subject: 'Changes pushed to branch ${{ github.ref }}'
        body: 'Changes have been pushed to branch ${{ github.ref }}'
        to: 'tirzahpeniche@gmail.com'
        from: Tirzah y Ferdi